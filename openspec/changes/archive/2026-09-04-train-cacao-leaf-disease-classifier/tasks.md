## 1. Dataset Acquisition

- [x] 1.1 Download and inspect the Amini Cocoa Contamination Dataset (Kaggle: ohagwucollinspatrick/amini-cocoa-contamination-dataset) as the primary labeled leaf source, and verify its class list, image counts per class, and license terms are documented — DONE: CC BY 4.0; Train.csv has 5,529 unique labeled leaf images across 3 classes (healthy 4,280 / cssvd 3,241 / anthracnose 2,271 bounding-box annotations); official Test.csv (1,626 images) has no ground truth (competition submission set) so a held-out test split must be carved from Train.csv instead
- [x] 1.2 Download the full Amini `dataset/images/train/` image set (~5,529 images) and collapse each image's bounding-box annotations into a single whole-image label (majority/only class; document how the 6 mixed-class images are handled), and verify a manifest (image path → label → source → license) is produced for all 5,529 images — DONE: `data/amini/build_manifest.py` produces `data/amini/manifest.csv`; 6 mixed-class images resolved by majority vote; final label counts: cssvd 2,237 / healthy 1,726 / anthracnose 1,566
- [x] 1.3 Split the labeled Amini images into train/validation/test subsets with no overlap (since the dataset's own Test.csv has no ground truth), and verify a script or notebook cell asserts disjoint image sets across subsets and reports per-class counts per split — DONE: stratified 70/15/15 split with disjointness asserted in `build_manifest.py`; per-split per-class counts recorded in `manifest.csv`
- [ ] 1.4 (Stretch, optional) Query the iNaturalist API (`api.inaturalist.org`, taxon Theobroma cacao, `photos=true`, paced to ~1 req/sec) for supplementary leaf photos, download images filtered to permissive licenses, manually label them (healthy/cssvd/anthracnose, reconciled with the Amini scheme), and verify a label count summary is produced — only pursue if time remains after 2–4 are done, since Amini alone is sufficient to train and evaluate the model

## 2. Model Training

Implemented as a self-contained Kaggle Notebook (`notebooks/train_kaggle.ipynb`, originally scoped for Colab, switched to Kaggle in practice) so training runs on free GPU instead of local CPU.

- [x] 2.1 Set up the training environment (Kaggle auth, dependencies) and verify the notebook's setup cells run end-to-end without error — DONE: ran on Kaggle GPU, `notebooks/train_kaggle.ipynb` §1–2, Kaggle Secrets auth confirmed working
- [x] 2.2 Implement data loading with augmentation for the training subset and verify a batch of transformed images/labels can be inspected — DONE: `notebooks/train_kaggle.ipynb` §3–4 ran successfully; batch shape confirmed `torch.Size([32, 3, 224, 224])`, train/val/test = 3869/830/830
- [x] 2.3 Implement transfer learning training loop (EfficientNet-B0, frozen backbone, classifier head fine-tuned) and verify training completes and a checkpoint (best val accuracy) is saved — DONE: 15 epochs on Kaggle GPU (~55 min total), best validation accuracy 79.9% (epoch 14), in-memory best-state checkpoint restored into `model` after training
- [x] 2.4 Implement evaluation on the test subset reporting accuracy and per-class precision/recall, and verify the evaluation report is generated and includes explicit caveats for any class below the documented minimum sample count (50) — DONE: test accuracy 77.8% (830 held-out images); healthy 0.701/0.842/0.765, cssvd 0.815/0.747/0.780, anthracnose 0.839/0.753/0.794 (precision/recall/F1); all classes ≥200 test samples, no low-confidence caveats triggered
- [x] 2.5 Save the final model artifact plus its class label mapping, and verify the artifact can be reloaded and produces the same predictions on a held-out sample — DONE: `models/cacao_leaf_classifier.pt`, `models/label_mapping.json`, `models/eval_report.json` in place; `models/verify_reload.py` reloads the architecture from `label_mapping.json`, loads the weights, and re-evaluates on the same disjoint test split — reproduced test accuracy 0.7783 (646/830), exactly matching `eval_report.json`'s reported accuracy (diff 0.0000)

## 3. Inference Service

- [x] 3.1 Implement a prediction endpoint that accepts an image upload and returns predicted label plus confidence, and verify it returns a correct response for a known sample image — DONE: `src/inference_service/main.py` (`POST /predict`); `src/inference_service/verify_api.py` confirms a well-formed response (label + confidence + per-class probabilities), and bulk-checking the classifier against the full local test split reproduces 0.7783 accuracy (646/830), matching the training-time evaluation exactly
- [x] 3.2 Implement input validation for unsupported file types or missing files, and verify the endpoint returns a clear error response instead of crashing — DONE: rejects non-JPEG/PNG content types (400) and undecodable files (400) without crashing; verified in `verify_api.py`
- [x] 3.3 Load the model artifact and label mapping from training output at service startup, and verify the served label mapping matches the training artifact's mapping — DONE: `src/inference_service/model.py`'s `LeafClassifier` loads `models/label_mapping.json` and `models/cacao_leaf_classifier.pt` at startup; `/health` exposes the loaded class order (`["healthy","cssvd","anthracnose"]`) matching the artifact

## 4. Upload Frontend

- [x] 4.1 Build a minimal page with an image upload control that calls the inference service, and verify a manual upload displays the predicted label and confidence — DONE: `frontend/index.html` + `frontend/static/{app.js,style.css}`, served same-origin by `src/inference_service/main.py` (`GET /`, `/static/*`); verified in a real headless Chrome (Playwright) session — uploading a sample leaf image renders "Resultado: Sadia" / "Confiança: 93.6%" in `#result`
- [x] 4.2 Add error display for failed predictions (unsupported file, service error), and verify a manual test with an invalid file shows a clear message instead of a blank/broken result — DONE: verified in the same browser session — uploading a `.txt` file renders the API's 400 error message in `#error` instead of a blank/broken result
- [x] 4.3 Verify the end-to-end flow works with no login/authentication required — DONE: no auth of any kind exists on `/`, `/static/*`, or `/predict`; both browser test cases above completed with no login step

## 5. Course Deliverables

- [x] 5.1 Complete the ML/Data Project Canvas (problem, data sources, target, features, data products, pitch) using this proposal/design as source material, and verify it is ready to present — DONE: `docs/ml_canvas.md`, all 6 blocks filled in with the real dataset, model, and result
- [x] 5.2 Document dataset limitations, model evaluation caveats, and next steps for a potential future integration with the larger CacauClima initiative — DONE: `docs/limitations_and_next_steps.md`
